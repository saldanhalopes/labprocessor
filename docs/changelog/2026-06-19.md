# Changelog — 2026-06-19

## Phase 1: Migração para PostgreSQL + Docker (Executada)

### Tasks Completas
- **1.1** Schema PostgreSQL com pgvector criado
- **1.2** Camada postgres.js substituindo firestore.js
- **1.3** Auth nativa com bcrypt + JWT
- **1.4** Busca vetorial com pgvector (substitui Pinecone)
- **1.5** Storage local (substitui Google Cloud Storage)
- **1.6** Stripe removido do frontend
- **1.7** JWT integrado no frontend
- **1.8** Dockerfile + docker-compose criados
- **1.9** Script de migração Firestore → PostgreSQL
- **1.10** Cleanup de arquivos legados

### Arquivos Removidos
- `backend/firestore.js` → substituído por postgres.js
- `backend/pinecone.js` → substituído por pgvector.js
- `backend/utils/imageExtractor.js` → não utilizado
- `firebase.json`, `apphosting.yaml`, `.firebaserc` → substituídos por Docker

### Arquivos Criados
- `backend/postgres.js`, `backend/pgvector.js`, `backend/auth.js`
- `backend/db/schema.sql`, `backend/db/init.sql`
- `backend/migrate.js`
- `frontend/services/api.ts`
- `Dockerfile`, `docker-compose.yml`, `.dockerignore`

### Build
- Frontend compila sem erros (2551 módulos)
- Dependências instaladas via npm workspaces

## Quick Task: Docs Inicializado

- `docs/` criado com estrutura de documentação do projeto
- `docs/README.md` com índice de navegação
- `docs/gsd-planning/` com artefatos GSD arquivados
- `docs/changelog/` com histórico de mudanças

## Fix: ivfflat Index e Docker Compose

- ivfflat removido (pgvector limita 2000 dims, Gemini usa 3072)
- `version` obsoleto removido do docker-compose.yml
- **Projeto rodando localmente via Docker** 🟢

## Como acessar

- **App:** http://localhost:8080
- **Login:** admin / admin
- **DB:** localhost:5432 (labprocessor/labprocessor)

## Atualizações Realizadas

- Docker compose re-build com schema.sql corrigido (ivfflat comentado)
- Volume de dados recriado (down -v + up)
- Testado: health endpoint OK, DB connected, admin user created
